#include <iostream>
using namespace std;

int main() {
    int arr[] = {1, 0, 1, 0, 1};

    int size = sizeof(arr) / sizeof(arr[0]);

    int left = 0;
    int right = size - 1;

    for (int i = 0; i < size; i++) {
       
    }

    return 0;
}


















